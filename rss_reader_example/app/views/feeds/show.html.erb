<p>
  <b>Url:</b>
  <%= @feed.url %>
</p>

<p>
  <b>Title:</b>
  <%= @feed.title %>
</p>

<% @feed.posts.each do |post| %>
  <div id="posts">
    <%= link_to raw(post.title), '#' %>
  </div>
<% end %>


<%= link_to 'Edit', edit_feed_path(@feed) %> |
<%= link_to 'Back', feeds_path %>
